export declare enum FilterOperator {
    EQUALS = "$eq",
    NOT_EQUALS = "$ne",
    GREATER_THAN = "$gt",
    GREATER_THAN_OR_EQUALS = "$gte",
    LOWER_THAN = "lt",
    LOWER_THAN_OR_EQUALS = "$lte",
    STARTS_WITH = "$starts",
    ENDS_WITH = "$ends",
    CONTAINS = "$cont",
    EXCLUDES = "$excl",
    IN = "$in",
    NOT_IN = "$notin",
    IS_NULL = "$isnull",
    NOT_NULL = "$notnull",
    BETWEEN = "$between",
    EQUALS_INSENSITIVE = "$eqL",
    NOT_EQUALS_INSENSITIVE = "$neL",
    STARTS_WITH_INSENSITIVE = "$startsL",
    ENDS_WITH_INSENSITIVE = "$endsL",
    CONTAINS_INSENSITIVE = "$contL",
    IN_INSENSITIVE = "$inL",
    NOT_IN_INSENSITIVE = "$notinL"
}
