"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
describe('SortOrder', () => {
    it('should accept "asc" as a valid value', () => {
        const order = 'asc';
        expect(order).toBe('asc');
    });
    it('should accept "desc" as a valid value', () => {
        const order = 'desc';
        expect(order).toBe('desc');
    });
    it('should not accept other things than "asc" or "desc" as valid value', () => {
        const order = 'invalid';
        expect(typeof (order)).not.toBe('SortOrder');
    });
});
//# sourceMappingURL=sort-order.spec.js.map