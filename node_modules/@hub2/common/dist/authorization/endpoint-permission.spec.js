"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
describe('EndpointPermission interface', () => {
    let endpointPermission;
    beforeEach(() => {
        endpointPermission = {
            scope: 'testScope',
            action: 'testAction',
        };
    });
    it('should be defined', () => {
        expect(endpointPermission).toBeDefined();
    });
    it('should have 2 properties', () => {
        expect(Object.keys(endpointPermission).length).toEqual(2);
    });
    it('should have the good types for properties', () => {
        expect(typeof endpointPermission.scope).toBe('string');
        expect(typeof endpointPermission.action).toBe('string');
    });
});
//# sourceMappingURL=endpoint-permission.spec.js.map