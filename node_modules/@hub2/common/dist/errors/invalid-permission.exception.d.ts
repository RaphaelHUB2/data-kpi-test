import { PermissionTarget } from '../types';
export declare class InvalidPermissionException {
    permissionIds: string[];
    target: PermissionTarget;
    constructor(permissionIds: string[], target: PermissionTarget);
}
