"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const sort_1 = require("./sort");
describe('Sort', () => {
    it('properties of the Sort object should be the expected properties', () => {
        const queryString = 'age,desc';
        const sortObject = new sort_1.Sort(queryString);
        expect(sortObject.field).toBe('age');
        expect(sortObject.order).toBe('desc');
        expect(sortObject.isInvalid).toBe(undefined);
    });
    it('should set sortObject.isInvalid to true because field is not provided', () => {
        const queryString = ',desc';
        const sortObject = new sort_1.Sort(queryString);
        expect(sortObject.isInvalid).toBe(true);
    });
    it('should set sortObject.isInvalid to true because the provided order is unknown', () => {
        const queryString = 'field, ascii';
        const sortObject = new sort_1.Sort(queryString);
        expect(sortObject.isInvalid).toBe(true);
    });
    it('should set sortObject.order to asc by default because the order is not provided', () => {
        const queryString = 'field,';
        const sortObject = new sort_1.Sort(queryString);
        expect(sortObject.isInvalid).toBe(undefined);
        expect(sortObject.order).toBe('asc');
    });
});
//# sourceMappingURL=sort.spec.js.map