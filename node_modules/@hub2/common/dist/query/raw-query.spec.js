"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
describe('RawQuery', () => {
    const rawQuerySimple = { filter: 'age=20', sort: 'asc', limit: '10', perPage: '2', page: '5' };
    const rawQueryTable = { filter: ['age=20', 'gender=female'], sort: ['asc', 'desc'], limit: '10', perPage: '2', page: '5' };
    it('should have the expected properties', () => {
        expect(rawQuerySimple.filter).toEqual('age=20');
        expect(rawQuerySimple.sort).toEqual('asc');
        expect(rawQuerySimple.limit).toEqual('10');
        expect(rawQuerySimple.perPage).toEqual('2');
        expect(rawQuerySimple.page).toEqual('5');
        expect(rawQueryTable.filter).toEqual(['age=20', 'gender=female']);
        expect(rawQueryTable.sort).toEqual(['asc', 'desc']);
        expect(rawQueryTable.limit).toEqual('10');
        expect(rawQueryTable.perPage).toEqual('2');
        expect(rawQueryTable.page).toEqual('5');
    });
    it('should miss some optional properties', () => {
        const missProperties = { filter: 'age=20', sort: 'asc' };
        expect(missProperties).toHaveProperty('filter');
        expect(missProperties).toHaveProperty('sort');
        expect(missProperties).not.toHaveProperty('limit');
        expect(missProperties).not.toHaveProperty('perPage');
        expect(missProperties).not.toHaveProperty('page');
    });
});
//# sourceMappingURL=raw-query.spec.js.map