import { Dictionary } from '../types';
export declare namespace EnumsHelper {
    function getEnumFromValue<E>(value: string, enumType: Dictionary<E>): E | undefined;
    function getValues(enumType: Dictionary<string>): string[];
}
