export declare class Paged<T> {
    readonly data: T[];
    readonly totalItems: number;
    readonly currentPage: number;
    readonly countPerPage: number;
    constructor(data: T[], totalItems: number, currentPage: number, countPerPage: number);
    getContentRange(): string;
}
