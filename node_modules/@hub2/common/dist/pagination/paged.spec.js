"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const paged_1 = require("./paged");
describe('Paged', () => {
    it('should be instantiated with the following arguments', () => {
        const data = [1, 2, 3];
        const totalItems = 5;
        const currentPage = 1;
        const countPerPage = 2;
        const paged = new paged_1.Paged(data, totalItems, currentPage, countPerPage);
        expect(paged).toBeInstanceOf(paged_1.Paged);
        expect(paged.data).toEqual(data);
        expect(paged.totalItems).toEqual(totalItems);
        expect(paged.currentPage).toEqual(currentPage);
        expect(paged.countPerPage).toEqual(countPerPage);
    });
    describe('getContentRange', () => {
        it('should return a string with offset = 2, limit = 3 and totalItems = 5', () => {
            const data = [1, 2, 3, 4, 5];
            const totalItems = 5;
            const currentPage = 2;
            const countPerPage = 2;
            const paged = new paged_1.Paged(data, totalItems, currentPage, countPerPage);
            const expected = '2-3/5';
            expect(paged.getContentRange()).toBe(expected);
        });
    });
});
//# sourceMappingURL=paged.spec.js.map