"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Paged = void 0;
class Paged {
    constructor(data, totalItems, currentPage, countPerPage) {
        this.data = data;
        this.totalItems = totalItems;
        this.currentPage = currentPage;
        this.countPerPage = countPerPage;
    }
    getContentRange() {
        const offset = this.currentPage === 1 || this.countPerPage === -1 ? 0 : (this.currentPage - 1) * this.countPerPage;
        let limit = this.countPerPage ? this.currentPage * this.countPerPage - 1 : this.totalItems;
        const totalItems = this.totalItems;
        if (limit > totalItems) {
            limit = totalItems;
        }
        return `${offset}-${limit}/${totalItems}`;
    }
}
exports.Paged = Paged;
//# sourceMappingURL=paged.js.map